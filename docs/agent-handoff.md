# Handoff для агентов

Что нужно знать, прежде чем править этот проект. Здесь только то, что **не
выводится из чтения кода**: договорённости, ловушки, на которых уже теряли
время, и текущее состояние работ. Порядок работы с submodule —
[development-workflow.md](development-workflow.md), конвенции —
`tourism-platform/docs/development-conventions.md`.

Файл зеркалится на GitHub вместе с суперпроектом, поэтому **адресов серверов,
ключей и внутренних ссылок здесь нет**. Всё это — в
`tourism-platform/docs/ci-and-runners.md` (лежит только локально: в
`tourism-platform` весь `docs/*` в gitignore).

## Из чего состоит репозиторий

Суперпроект с тремя submodule:

| Модуль | Что это | Стек |
| --- | --- | --- |
| `tourism-backend` | API, админка, ИИ-подбор, RAG | FastAPI, SQLAlchemy, Alembic, PostGIS, Redis |
| `tourism-mobile` | приложение | Flutter, Riverpod |
| `tourism-platform` | документы, планы, решения | markdown, **не публикуется** |

Изменения коммитятся в дочерний репозиторий, потом в суперпроекте обновляется
SHA submodule отдельным коммитом `chore: bump <repo> to <sha>`.

## Договорённости с владельцем проекта

- **Сообщения коммитов — по-английски**, документы и история изменений — по-русски.
- **Деплой запускает владелец руками.** Job `backend-deploy-production` в GitLab
  ручной; агент его не триггерит, даже когда правка «очевидно готова». Сборку
  мобильного приложения владелец тоже делает сам.
- Правки на проде через SSH допустимы для файлов, которых нет в git
  (`deploy-remote.sh`, `compose.yaml` на сервере), но о них нужно сказать вслух —
  их не подхватит ни один MR.
- Прежде чем удалять данные на проде — показать, что именно удаляется, построчно
  с владельцем и содержимым. Однажды по совпадению имён были снесены реальные
  маршруты пользователя.

## Проверка перед коммитом

```bash
# backend
cd tourism-backend
uv run ruff check src tests && uv run ruff format --check src tests
uv run mypy src/tourism_backend
uv run pytest tests/unit -q --no-cov

# mobile
cd tourism-mobile
flutter analyze
flutter test
```

**7 упавших тестов и ~40 ошибок в `tests/unit` — это норма без локальной базы**
(они ходят в PostGIS). Сверяйтесь с чистым деревом (`git stash`), прежде чем
чинить «сломанное». Полный прогон бэкенда с базой — `./scripts/validate.sh`.

Пиксельные golden-тесты идут **только на macOS** (`_skipPixelGoldens`), там же и
перегенерируются: `flutter test --update-goldens --plain-name "<имя>"`.

## Ловушки, на которых уже теряли время

**Flutter-тесты считают платформу Android.** `defaultTargetPlatform` в
`flutter_test` прибит к Android, и `ThemeData(platform: iOS)` его не меняет. Код,
который ветвится через `AppPerf`, в тестах пойдёт по андроидной ветке, а тест
будет «зелёным», ничего не проверив. Нужен
`debugDefaultTargetPlatformOverride`, и сбрасывать его — в `try/finally` внутри
теста: `addTearDown` отрабатывает **после** проверки биндинга и падает с «foundation
debug variable was changed».

**`CustomPaint` поверх интерактивного слоя съедает нажатия.** Декоративный
оверлей во весь `Stack` (подсветка пройденного маршрута) отбирал тапы у карты под
ним. Такой слой всегда заворачивать в `IgnorePointer`.

**`tester.tap` не воспроизводит гонки внутри жеста.** Он шлёт down и up без кадра
между ними, поэтому баг «виджет исчез между нажатием и отпусканием» тест не
поймает. Нужен `startGesture` → `pump(80ms)` → `up()`. Так был найден баг с
историей поиска: снятие фокуса перестраивало дерево и уносило строку до того,
как срабатывал `onTap`.

**Finder'ы пропускают offstage.** После `Navigator.push` предыдущий экран
остаётся смонтированным, но `find.*` его не видит (`skipOffstage: true`), а
`find.bySemanticsLabel` работает по дереву семантики и не найдёт узел, обрезанный
`ClipRect`. Если проверяете конкретный экран — сужайте поиск через
`find.descendant`.

**Переменные окружения заводятся в двух местах.** Мало добавить переменную в
GitLab: её нужно вписать и в `scripts/ci-deploy-production.sh`, и в серверный
`deploy-remote.sh` (его в git нет). Плюс значение из `.env` **не попадает** в
контейнер, пока не указано в блоке `environment:` в `compose.yaml` на сервере.
Подробности и список — в `ci-and-runners.md`.

**CI-постгрес может упасть без единой правки в коде.** `.postgres-service` в
`tourism-backend/.gitlab-ci.full.yml` перед стартом ставит pgvector через
`apt-get update && apt-get install ... && exec docker-entrypoint.sh`
(прод-образ с pgvector собирается в соседнем репозитории, этот job его
собрать не может). У `postgis/postgis:16-3.4` протухает по времени Release
file Debian bullseye-security — `apt-get update` падает, `&&`-цепочка
обрывается, `docker-entrypoint.sh` не запускается, postgres не слушает порт,
и `backend-run-tests`/`backend-security-tests` падают через 180 секунд с
«postgres did not become reachable», хотя вчера тот же пайплайн был зелёным
на том же коде. Чинится флагом `-o Acquire::Check-Valid-Until=false` у
`apt-get update` (добавлено 2026-09-08) — если снова всплывёт похожая ошибка
про expired Release file, смотреть сюда раньше, чем в диф последнего коммита.

**История изменений проверяется тестами.** `lib/features/settings/domain/changelog.dart`
живёт в приложении, а не приходит с сервера — это осознанное решение: запись
описывает конкретную сборку. Тесты требуют: 20–300 символов на запись, никаких
слов из реализации, версии сортируются **как строки** (поэтому `0.2.31` выше
`0.2.3`), «готовится» ровно одна версия и только она без даты.

## Как устроены вещи, которые выглядят иначе, чем ожидаешь

**Карта — это растр, а не SDK.** Бэкенд отдаёт картинку 2ГИС с нарисованным
маршрутом, а приложение само кладёт поверх пины, точку «вы здесь» и подсветку
пройденного, считая координаты той же меркаторовской математикой
(`map_projection.dart`). Никакого интерактивного map SDK в проекте нет. Идея
перерисовывать дорожную геометрию самим на тайлах OSM обсуждалась и отложена.

**Один ход ИИ-чата — это два запроса к модели** (ответ, запрос инструмента,
ответ), 7–17 секунд на проде. Отсюда параметры: `ai_request_timeout_seconds` — на
**один** вызов, `ai_turn_budget_seconds` — на весь ход; если бюджет кончается на
втором вызове, отдаётся ответ от первого. У мобильного клиента отдельный таймаут
60 с на эти запросы, у остальных остаётся 20 с.

**RAG греется при входе в чат.** Первая загрузка модели эмбеддингов — 9.5 секунды
(потом 0.05), и раньше она случалась внутри первого хода, прямо в event loop,
подвешивая весь воркер. Теперь загрузка уходит в поток и запускается фоном при
создании/открытии сессии, пока человек печатает. Контекст модели ограничен
последними 12 репликами, длина диалога — `ai_chat_message_limit`, срок жизни —
`ai_chat_session_ttl_hours` (закрывается лениво при следующем заходе).

## Текущее состояние

- **Commit/push — 2026-09-09, по запросу владельца:** backend `71fe130`
  (диалог по цели + RAG), предыдущий `ece37d5` (поиск справки и MiniLM);
  mobile `e7d17e6` (карточка справки и доступность чата). Дочерние `main`
  опубликованы в GitLab и GitHub-зеркалах. Ниже «локально / без push» —
  исторический статус этапов, заменённый этой записью.
- Перед ручным обновлением backend применить миграции до
  `0057_support_help_embeddings` (включая `0056`). Статьи остаются draft,
  release/редакционное одобрение не подтверждены; семантический флаг справки
  по умолчанию выключен. Не путать выкладку кода с публикацией корпуса.
- Перед push повторно прошли Ruff/format, mypy, проверка экспорта 15 FAQ,
  572 backend unit-теста без пяти DB-зависимых модулей, Flutter analyze и
  40 mobile-тестов. Полный PostGIS/live-provider прогон не подтверждён.
- Версия приложения `0.2.31+22`, в истории изменений помечена как готовящаяся
  (`inProgress: true`, без даты). При выпуске — заменить флаг на дату.
- **Бэкенд не задеплоен.** Лимиты чата, прогрев RAG, бюджет хода, починка списка
  уведомлений и перестановка разделов админки ждут ручного деплоя.
- Суперпроект синхронизирован с проверенным инкрементом backend/mobile и
  текущим `tourism-platform`; внутренние рабочие планы остаются локальными.

### Инкремент — 2026-09-08

- В backend/mobile зафиксирован инкремент по скриншотам владельца:
  контекстные quick replies, однократное подтверждение controls, каталог перед
  генерацией, профиль в параметрах диалога, актуальный вопрос в RAG, исправления
  карточки и обычный iOS pop из чата к форме.
- Предпросмотр предложения: приватные `GET …/proposals/{id}/preview` и `/map`,
  `PATCH …/trip-date`. Не принимает предложение и не расходует новую квоту.
  **Перед запуском обновлённого backend нужна миграция `0055_proposal_preview`.**
- План по дням предварительный: пути, посещения, перерывы, потребность в ночлеге,
  выбор даты. Конкретные отели/кафе не подбираются; в прохождении пока прежние
  точки. План сохраняется в `Route.accessibility.trip_plan` при принятии.
- Коммиты отправлены в GitLab `origin/main`: backend `62e6074`, mobile `a5a16ea`.
  В версии `0.2.31` добавлены пользовательские записи об инкременте;
  флаг `inProgress` не снят. Переключение модели и deploy не делались.
- Собственная сборка тоже учитывает бюджет/людность: известная минимальная
  цена выше дневного бюджета в RUB исключает место; «бесплатно»/нулевой бюджет
  исключают заведомо платное. Неизвестные цены не считаются бесплатными и
  помечаются в расписании. Это пока не полная смета по дням.
- Для повторной сборки точки последних трёх предложений **этой сессии и
  пользователя** получают небольшой штраф ранжирования. Это не жёсткое
  исключение: маленький каталог не ломается, ограничения и интересы важнее.
- Backend: **556 тестов прошли** (`tests/unit` + security planning sessions и
  generate), Ruff и mypy прошли. Тесты запускались с локальными PostGIS/Redis.
  Для проверки отсутствующего ключа DeepSeek переопределять `DEEPSEEK_API_KEY=`:
  иначе одноимённый тест подхватывает реальный ключ из локального `.env`.
- Mobile: **481 тест прошёл** при `flutter test --concurrency=1`, включая
  macOS goldens; `flutter analyze` без замечаний. В параллельном прогоне один
  раз упал `image_cache_security_test` (отдельно и последовательно проходит).
  Сам кеш в этом инкременте не менялся; нестабильность параллельного теста
  не считать устранённой. Для golden чата явно прогревается `route_header.jpg`,
  иначе снимок мог фиксировать серую шапку до загрузки asset.
- Временный Redis для тестов удалён, локальный PostgreSQL снова остановлен.
  Его данные сохранены, миграции не откатывались. Другие сервисы не менялись.
- Подробный статус, бизнес-ограничения расписания и план ИИ-поддержки:
  `tourism-platform/docs/ai-travel-agent-trip-plan-2026-09-08.md` (локальный документ).

### Уточнение приоритета — 2026-09-08

- Владелец отложил отели и кафе до post-MVP и предложил сменить фокус.
  Не продолжать расширение планировщика вместо оставшихся задач текущего MVP;
  срок календарного прохождения отдельно не согласован.
- Спроектирован помощник поддержки по опубликованной справке: источники,
  реальные события инструментов и передача оператору в том же обращении.
  Подробности: `tourism-platform/docs/support-ai-mvp-design-2026-09-08.md`
  (локальный документ). ИИ-помощник ещё не реализован.
- Первый срез SUP-01: 15 статей в `tourism-backend/data/support_help`,
  typed manifest и проверка `scripts/check_support_help.py`;
  `--check-mobile` сверяет экспорт с `support_faq_content.dart`.
  Это единый источник FAQ; generated Dart не редактировать вручную.
  Исправлены устаревшие обещания, получатель баллов и обязательность точек.
  Длинные статьи читаются полностью; неизвестный ID не подменяется первым FAQ.
- Корпус сверён с кодом для готовящейся `0.2.31`, но остаётся draft:
  release и редакционное одобрение не подтверждены. Нынешний чат поддержки
  по-прежнему отвечает статическим приветствием. Поисковый срез описан ниже;
  генеративные ответы пока не добавлены.
  Не индексировать весь внутренний `docs/`. Подробный локальный аудит:
  `tourism-platform/docs/support-faq-audit-2026-09-08.md`.
- Проверки среза справки: 20 backend-тестов source pack, 23 целевых
  mobile-теста, Ruff/format, mypy, analyze, Markdown lint и совпадение
  экспорта пройдены. Полные DB/security-прогоны не повторялись: API и
  схема БД не менялись. Срез пока локальный, без commit/push/deploy.
- Показ действий из референса применим и к тревел-агенту, но только по
  фактическим событиям сервера. Это не текст скрытых рассуждений модели.

### Поисковый помощник справки — 2026-09-08, локальный срез

- По уточнению владельца первым реализован простой поиск инструкций.
  Предусмотренная в RAG MiniLM — энкодер для смыслового поиска, не чат-модель.
  В поддержку она пока не подключена; рабочую конфигурацию не переключали.
- **Перед запуском обновлённого backend нужна миграция `0056_support_help`.**
  Отдельная таблица `support_help_revisions`, неизменяемый текст редакции,
  транзакционный импорт `scripts/import_support_help.py`. По умолчанию
  preview без DB; запись только через `--apply` и согласованную публикацию.
- Поиск — русский PostgreSQL FTS, до трёх статей. Только published/ru,
  точная версия приложения, наступившая публикация и неистёкшая проверка.
  Повторный импорт не продлевает срок; новая редакция отзывает предыдущую
  для этой версии. Удаление из manifest само по себе не отзывает статью.
- Публичные `POST /api/v1/support/help/search` (q/version в JSON) и
  `GET /api/v1/support/help/{article_id}` (revision/app_version). Вопрос
  не передаётся в URL; публичные статьи не дают доступа к private tickets.
  Нет индексации внутреннего docs, запросов к LM или общего кеша ответов.
- Mobile открывает точную редакцию повторным запросом. При отзыве нет
  подмены текстом из bundled FAQ. Отдельный офлайн-FAQ остаётся доступен.
  «Написать оператору» только заполняет поле чата, отправляет пользователь.
  Полноценный handoff с просмотренными источниками и assistant runs впереди.
- Статьи по-прежнему draft; live-публикация не выполнялась. Backend-справка
  в рабочем дереве; mobile уже включён другим потоком работы в `e99f210`.
  В этом продолжении агент не делал commit/push/deploy, чужие коммиты
  не переписывал. Ручной smoke на устройстве и одобрение владельцем впереди.
- 65 backend-тестов (20 source pack, 45 интеграционных) и 34 целевых
  mobile-теста прошли; Ruff/format, mypy, analyze, Markdown lint и экспорт FAQ
  тоже. Полные тесты проекта не повторялись. Для новой таблицы сравнение
  metadata/migration проходит; общий `alembic check` показывает расхождения
  вне этой таблицы, включая служебные PostGIS-таблицы. Их не правили.
- После тестов таблица справки пустая; PostgreSQL снова остановлен,
  его данные сохранены, миграция не откатывалась. Подробности:
  `tourism-platform/docs/support-help-search-2026-09-08.md`.

### Пробный гибридный поиск MiniLM — 2026-09-08/09, локально

- Владелец согласовал подключение MiniLM к поиску справки. Реализовано под
  `SUPPORT_HELP_SEMANTIC_ENABLED=false` по умолчанию. Генерации ответа нет.
  Используется прежний provider и кеш весов RAG; `hash-v1` не подходит.
  Рабочий `RAG_EMBEDDING_MODEL` ради этой задачи не переключался.
- **Нужна миграция `0057_support_help_embeddings` после `0056`.** Индекс
  отдельный: редакция/model/profile/hash, 384-мерные массивы коротких абзацев.
  Длинный текст разбивается tokenizer-окнами с перекрытием. Индексация
  `scripts/index_support_help.py --app-version …` по умолчанию только читает
  DB; `--apply` явно записывает индекс опубликованных неистёкших редакций.
- FTS + semantic объединяются по рангам, до трёх статей. Model/profile/hash,
  публикация, версия и сроки проверяются до выдачи; отзыв перепроверяется
  после вычисления вопроса и при открытии. При сбое/отсутствии индекса,
  модели, таймауте или занятом encoder остаётся FTS. Не копить CPU-очередь:
  отмена asyncio-ожидания не останавливает уже запущенный поток torch.
- Порог 0.55 выбран на development; на отдельном синтетическом контроле
  нужная статья в top-3: 8/15 у гибрида против 5/15 у FTS. Посторонняя выдача
  осталась 3/10 у обоих. На 40 прежних регрессиях: 28/28 попаданий,
  0/12 посторонних выдач у обоих. Это ограниченный прирост, не готовая
  замена оператора и не независимая оценка на реальных обращениях.
- В Mobile добавлено «Уточнить вопрос» без нового запроса/автоотправки.
  Карточки обозначены как возможные инструкции, не гарантированное решение.
- В этом срезе нет commit/push/deploy и live-публикации статей. Перед
  включением нужны одобрение, проверка выпуска, индекс и замер на хосте.
  Новые support-переменные не подключены к удалённому Compose/deploy wrapper;
  просто заполнить `.env` недостаточно. Не менять удалённые файлы молча.
- Подробный статус и результаты:
  `tourism-platform/docs/support-help-minilm-2026-09-08.md`.
- Финальные проверки среза: 92 целевых backend-теста и 35 mobile-тестов,
  Ruff/format, mypy, analyze и Markdown lint прошли. Opt-in тест настоящей
  модели отдельно пройден на development, контроле и прежних регрессиях;
  обычный прогон его пропускает и не требует весов. Полные тесты проекта
  не повторялись. Локально применена `0057`, тестовые данные откатились.
  Обе help-таблицы после тестов пустые. PostgreSQL снова остановлен;
  его прежние данные и применённые миграции сохранены.

### Район поиска вместо обязательного старта — 2026-09-09, локально

- По просьбе владельца **следующий этап качества поиска справки отложен**:
  уменьшить ложные совпадения FTS/гибрида, расширить проверенные формулировки,
  повторить независимый контроль. Ближайший backlog — в `progress.md`;
  статьи не публиковать и MiniLM-флаг автоматически не включать.
- Поиск справки оформлен одной белой карточкой с теми же радиусом/тенью,
  что у настроек; поле, кнопка, результаты и оператор внутри. Есть golden
  начального состояния/выдачи и проверка 320 px с масштабом текста 1.5.
- Реплика «Принял: … город старта?» соответствует backend fallback, а не
  доказывает, что её придумал DeepSeek. Убрано цитирование пользователя.
  Продовые логи/живой API в этом срезе не проверялись.
- В чате разделены `search_area`, мягкие `preferred_localities`,
  `flexible_start` и точный `city`. По ЮБК можно показать опубликованные
  маршруты без транспорта/длительности; черновые поля формы не фильтруют
  выдачу. `planning_mode` сохраняет переход к построению своего маршрута:
  там параметры по-прежнему нужны, генерации без явного выбора нет.
- У всех трёх адаптеров общий ограниченный JSON-контекст вместо обрезки
  сериализованного DATA до 1200 символов. Результаты инструментов тоже
  находятся в контексте: Gemini пропускал их прежние system-реплики.
  Передаются RAG, проверенные карточки каталога и мягкие предпочтения профиля.
  Не путать MiniLM-энкодер с генеративной моделью-турагентом.
- DeepSeek structured-запросы явно отключают thinking; лимит чат-ответа
  увеличен с 512 до 1024. Сбой JSON теперь отражён в `fallback`/телеметрии.
- Это не завершённая географическая поисковая система: район проверяется
  по locality точек, лимит загрузки каталога пока 200; сложные отрицания и
  неизвестная география передаются модели. Нужны live-eval DeepSeek/Gemini,
  контроль реального каталога, времени ответа и релевантности RAG.
- Подробности и QA: `tourism-platform/docs/travel-chat-discovery-2026-09-09.md`.
  120 целевых backend-тестов, 35 mobile-тестов, Ruff/format, mypy и analyze
  прошли. Полный unit-прогон: 553 passed, 7 failed / 40 errors без PostGIS;
  Docker daemon недоступен. Не считать это зелёным DB-интеграционным прогоном.
  Этот срез без commit/push/deploy, мобильную сборку делает владелец.

### Цель диалога и нижняя область поддержки — 2026-09-09, продолжение

- Убран shell bottom scrim на `/profile/settings` и вложенных экранах;
  сам floating nav и его оформление сохранены. Общий отступ настроек теперь
  учитывает `viewPadding.bottom`, поддержка имеет ещё 32 px запаса.
  Тест с реальным router/shell проверяет положение CTA выше nav и открытие чата.
- Сценарий определяется `goal` модели: discover / compare / place_info /
  custom / clarify, а не наличием распознанного города. `dialogue_goal`
  сохраняется в JSON сессии; старые модели совместимы через purpose-fallback.
  Эти поля не дают разрешения на генерацию.
- Discover без географии ищет в области приложения (Крым), но не записывает
  область как подтверждённый выбор/старт пользователя. Из первых идей убрана
  обязательная city/transport/duration-анкета, в том числе в legacy кнопке поиска.
  Для уточнения параметра модель возвращает `clarification_reason`;
  точный старт остаётся условием custom, а не просмотра каталога.
- После смыслового определения цели/пожеланий разрешён один ограниченный
  дополнительный вызов для ответа по фактически найденным карточкам.
  Данные уже подготовленного поиска не запрашиваются повторно при совпадении
  сигнатуры. При таймауте остаётся честный backend fallback.
- Compare использует последние показанные ID из своей сессии (окно истории
  ограничено), перепроверяя публичность маршрутов/точек. Не заменять их новым
  рейтингом и не доверять сохранённым карточкам как источнику актуального доступа.
  Place info не запускает анкету даже поверх незавершённого custom-плана.
- Новый нижний golden: `help_search_bottom.png`. Детали и QA:
  `tourism-platform/docs/travel-dialogue-goals-2026-09-09.md`.
  Итог: 572 backend unit-теста без пяти DB-зависимых модулей, 40 mobile-тестов,
  Ruff/format, mypy, analyze и Markdown lint прошли. Короткое «Да» после
  place_info не запускает каталог из-за заполненных старых параметров.
  Live API/eval и DB-интеграция не подтверждены, commit/push/deploy не выполнялись.

Новый чат теперь живёт на `/match/chat` над формой, а не переключается внутри
корневой страницы. Не возвращать туда `AppEdgeBackGesture`: он конфликтует
с интерактивным Cupertino-жестом. Legacy `/match/resume` возвращается к истории.
Проверка реального iOS-жеста — `test/features/route_match/chat_navigation_test.dart`.

Синтетическая схема допустима только с предупреждением. `overnight_needed`
не означает наличие отеля или безопасного места ночлега. Существующий
`длительность → число точек` ещё не заменён полноценным планировщиком поездки.

## Что осталось незакрытым

- Дипсик как провайдер ИИ не проверялся: переключается в админке, в разделе
  «Настройки приложения».
- Рейтинг маршрутов до сих пор считается по выдуманной формуле — см.
  `tourism-platform/docs/articles-mobile-rating-context-rag-backlog-2026-09-01.md`.
- Оценка качества RAG делалась на игрушечном наборе из 23 запросов; на реальном
  корпусе (469 фрагментов) не мерялась. Там же в документе — цифры и оговорки.
